generated: '2026-09-02'
method: generated
source: https://cloud.trisotech.com/help/des/system-integration/rest-api-documentation.html
name: Trisotech Agent Skills
summary: >-
  Four packaged Agent Skills for the Trisotech Digital Enterprise Suite Public API, each
  grounded in operationIds read verbatim from the provider's published API reference (129
  operations across 30 resource groups). No operationId, parameter or error code in these
  skills was invented; where the provider does not publish something — a reversal window, a
  rate limit, an idempotency key — the skill says so rather than filling the gap.
grounding:
  spec_available: false
  spec_note: >-
    The machine-readable OpenAPI sits behind the instance login at
    https://{instance}.trisotech.com/publicapi/doc (200, but redirects to /framebar/login).
    Skills are therefore grounded in the fully rendered public API reference, which publishes
    every operationId, path, parameter table, response-code table and model.
  operations_available: 129
  operationids_used: 40
skills:
  - name: trisotech-deploy-a-service
    file: skills/trisotech-deploy-a-service.md
    flow: Publish and deploy a BPMN/CMMN/DMN model as a running service
    scopes: [repo_r, mvn_r, mvn_w]
    write_surface: true
    reversible: true
  - name: trisotech-subscribe-to-events
    file: skills/trisotech-subscribe-to-events.md
    flow: Create a filtered event-emitter subscription over the CloudEvents catalog
    scopes: [emitter_r, emitter_w]
    write_surface: true
    reversible: true
  - name: trisotech-promote-models-with-governance
    file: skills/trisotech-promote-models-with-governance.md
    flow: Move models between places through the change-request workflow
    scopes: [repo_r, repo_w]
    write_surface: true
    reversible: partial
  - name: trisotech-find-and-route-user-tasks
    file: skills/trisotech-find-and-route-user-tasks.md
    flow: Search human tasks across environments, services and versions; delegate or reassign
    scopes: [bpmn_x, cmmn_x]
    write_surface: partial
    reversible: partial
shared_rules:
  auth: 'Authorization: Bearer <token> on every call; scopes come from the Client App grant list.'
  accept: 'Always send Accept: application/json — the default without it is deprecated XML.'
  errors: >-
    Branch on error[0].code, never on the HTTP status. A missing token returns HTTP 500 with
    code RequiresLogin.
  idempotency: >-
    None published. Never blind-retry a POST; re-read state first.
  rate_limits: >-
    None published and no headers returned. Bound your own concurrency.
  tracing: 'Capture the request-tracking-identifier response header.'
provider_published_skills: none
provider_published_skills_note: >-
  No AGENTS.md, skills/ directory or agent instruction file was found on trisotech.com,
  cloud.trisotech.com, or in the three public repositories of the Trisotech GitHub
  organization. These four skills are generated by API Evangelist, not published by Trisotech.
references:
  - conventions/trisotech-conventions.yml
  - errors/trisotech-problem-types.yml
  - scopes/trisotech-scopes.yml
  - asyncapi/trisotech-events.yml
